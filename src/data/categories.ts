import { Category } from '../types';

// Static categories array cleared per user request.
// Dynamic categories are managed via CategoryContext & database API.
export const CATEGORIES: Category[] = [];
